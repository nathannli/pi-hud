---
"pi-hud": patch
---

Update the pinned package manager to pnpm 11.4.0, align CI, publish, and contributor setup instructions, preserve pnpm 11's release-age gate with explicit temporary exclusions for the already-reviewed Pi 0.77.0 refresh, and document approved dependency build scripts required by the locked install.
