---
"pi-hud": patch
---

Render the startup notification through Pi's UI notification API instead of injecting a custom session message, preventing HUD startup text and release notes from entering agent prompt context.
